---
layout: layouts/base.njk
title: Blog
description: Writing on design, development, and building in public.
permalink: /blog/
---

<h1 class="text-4xl font-bold mb-2">{{ title }}</h1>
<p class="text-gray-600 mb-8">Short, practical posts on writing, tooling, and shipping on the web.</p>

<ul class="space-y-6">
{% for post in collections.posts | reverse %}
  <li class="border-b border-gray-200 pb-6">
    <h2 class="text-2xl mb-2 leading-tight">
      <a href="{{ post.url }}" class="text-blue-700 hover:text-blue-800 hover:underline">{{ post.data.title }}</a>
    </h2>
    <p class="text-sm text-gray-600 mb-2">
      <time datetime="{{ post.date | dateToRfc3339 }}">{{ post.date | readableDate }}</time> · {{ post.templateContent | readingTime }}
    </p>
    {% if post.data.description %}
    <p class="text-base text-gray-700 mb-3">{{ post.data.description }}</p>
    {% endif %}
    {% if post.data.tags %}
    <ul class="flex flex-wrap gap-2 mb-3 list-none p-0">
      {% for tag in post.data.tags %}
      {% if tag != 'posts' %}
      <li><a class="inline-flex px-2 py-1 rounded-full text-xs bg-gray-100 text-gray-700 hover:bg-gray-200" href="/tags/{{ tag | slugify }}/">#{{ tag }}</a></li>
      {% endif %}
      {% endfor %}
    </ul>
    {% endif %}
    <a href="{{ post.url }}" class="text-sm font-semibold text-blue-700 hover:text-blue-800">Read article →</a>
  </li>
{% else %}
  <li class="text-gray-600">No posts yet. Add one in <code>src/blog/</code> to get started.</li>
{% endfor %}
</ul>
